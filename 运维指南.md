# 运维指南

## 首次安装

```bash
cd D:\MyKnowledge\code\travel-planner

# 创建虚拟环境（仅首次）
python -m venv venv --system-site-packages

# 激活虚拟环境
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt --quiet
```

## 日常使用

```bash
# 激活虚拟环境（每次新终端都要执行）
venv\Scripts\activate

# 启动 Web 界面
streamlit run app.py --server.port 8501
```

浏览器打开 `http://localhost:8501`

## 后台爬虫 Worker

另开一个终端，挂着别关：

```bash
cd D:\MyKnowledge\code\travel-planner
venv\Scripts\activate
python pipeline_worker.py
```

按 Ctrl+C 退出。

## 跑测试

```bash
# 全量测试（~3 分钟）
pytest tests/ -v

# 只跑爬虫管道
pytest tests/test_pipeline.py -v
```

## 环境变量

复制 `.env.example` 为 `.env`，填入 DeepSeek API Key。其余不用改。

## 城市列表

`pipeline\cities.txt`，一行一个城市，按需增删。

## 目录结构

```
travel-planner/
  app.py                 Streamlit Web 界面
  pipeline_worker.py     独立爬虫 Worker
  pipeline/              数据管道模块
    cities.txt           全国城市列表
    crawler.py           爬取 + 正文提取 + 标题切分
    scheduler.py         定时调度
  agents/                4 个 LangChain Agent
  rag/                   ChromaDB 向量知识库
  tests/                 测试用例
```
